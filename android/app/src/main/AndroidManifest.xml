<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission"/>
    <!-- Offline-KI (Runde 13, Einheit 7): flutter_gemma downloads a
         multi-GB model file via a foreground service (background_downloader)
         so Android doesn't kill the download while backgrounded — these are
         "host-app responsibility" per flutter_gemma's own README, not
         auto-merged from the plugin's manifest. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <application
        android:label="jarvis_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Offline-KI model download (Runde 13, Einheit 7): API 34+
             requires a foregroundServiceType matching the FOREGROUND_SERVICE_
             DATA_SYNC permission above on WorkManager's shared foreground
             service, or startForeground() throws — see flutter_gemma's
             README "Foreground Service" section. tools:node="merge" adds
             the attribute onto the background_downloader/WorkManager-
             provided <service> instead of replacing it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
        <!-- Captures the redirect back into the app after Spotify login
             (see lib/services/spotify_service.dart / flutter_web_auth_2). -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="jarvismobile"/>
            </intent-filter>
        </activity>
        <!-- Benachrichtigungs-Zusammenfasser (Runde 14, Einheiten 6+8):
             "Benachrichtigungszugriff" ist eine besondere, vom Nutzer manuell
             in den System-Einstellungen erteilte Berechtigung
             (Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS) — kein normaler
             Laufzeit-Dialog. Erfasst nur, solange zusätzlich die
             "Benachrichtigungen erfassen"-Einstellung in der App aktiv ist. -->
        <service
            android:name=".NotificationListenerBridgeService"
            android:label="J.A.R.V.I.S. Benachrichtigungs-Zusammenfasser"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>
        <!-- Homescreen-Widget (Runde 14, Einheiten 7+9): rendert die von
             HomeWidgetService.refresh() geschriebenen Daten nativ (RemoteViews
             kann keine Flutter-Engine hosten). android:exported="true" ist
             hier Pflicht, da das System den APPWIDGET_UPDATE-Broadcast von
             außerhalb des eigenen Prozesses sendet. -->
        <receiver
            android:name=".JarvisWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/jarvis_widget_info"/>
        </receiver>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
