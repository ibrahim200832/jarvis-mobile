<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Parent kept as a Theme.AppCompat variant (originally required by
         local_auth_android's biometric prompt, Runde 15, Einheit 2, since
         removed in Runde 18) — left as-is, reverting is pure native-code
         risk for no functional benefit. -->
    <style name="LaunchTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
